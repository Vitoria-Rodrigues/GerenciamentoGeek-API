spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gerenciamentogeek
    username: root
    password: root
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    database: MYSQL
    show-sql: true
